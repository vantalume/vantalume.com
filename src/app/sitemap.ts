import type { MetadataRoute } from "next";import { articles,services,site } from "@/lib/site";export default function sitemap():MetadataRoute.Sitemap{const paths=["","/services","/how-we-work","/about","/opportunity-mapper","/insights","/contact","/privacy","/cookies","/terms",...services.map(x=>`/services/${x.slug}`),...articles.map(x=>`/insights/${x.slug}`)];return paths.map((path,i)=>({url:`${site.url}${path}`,lastModified:new Date("2026-07-11"),changeFrequency:i===0?"weekly":"monthly",priority:i===0?1:path.includes("privacy")||path.includes("cookies")||path.includes("terms")?.3:.8}))}
