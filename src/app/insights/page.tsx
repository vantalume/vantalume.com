import type { Metadata } from "next";import Link from "next/link";import { Arrow } from "@/components/icons";import { PageIntro } from "@/components/ui";import { articles } from "@/lib/site";
export const metadata:Metadata={title:"Insights",description:"Practical notes on websites, software, AI automation and better digital decisions.",alternates:{canonical:"/insights"}};
export default function Insights(){return <><PageIntro eyebrow="Insights" title="Make the decision before you buy the deliverable." intro="Practical field notes for leaders planning websites, products and automation—written to clarify trade-offs, not manufacture demand."/><section className="section shell"><div className="article-grid">{articles.map(x=><Link href={`/insights/${x.slug}`} key={x.slug} className="article-card"><span>{x.category} · {x.read}</span><h2>{x.title}</h2><p>{x.description}</p><Arrow/></Link>)}</div></section></>}
