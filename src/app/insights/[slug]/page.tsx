import type { Metadata } from "next";import { notFound } from "next/navigation";import Link from "next/link";import { articleBodies } from "@/lib/articles";import { articles } from "@/lib/site";
export function generateStaticParams(){return articles.map(x=>({slug:x.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const a=articleBodies[slug];if(!a)return{};return{title:a.title,description:a.dek,alternates:{canonical:`/insights/${slug}`},openGraph:{type:"article",title:a.title,description:a.dek}}}
export default async function Article({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const a=articleBodies[slug];if(!a)notFound();return <article><header className="article-hero shell"><nav aria-label="Breadcrumb"><Link href="/insights">Insights</Link><span aria-hidden="true"> / </span><span>Article</span></nav><p className="kicker">Field note · {a.published}</p><h1>{a.title}</h1><p className="lede">{a.dek}</p></header><div className="article-body shell">{a.sections.map(s=><section key={s.heading}><h2>{s.heading}</h2>{s.body.map(p=><p key={p}>{p}</p>)}</section>)}<aside><h2>Make the next decision clearer.</h2><p>Use the free Opportunity Mapper for a first-pass route, or tell us what needs to change.</p><p><Link className="text-link" href="/opportunity-mapper">Map an opportunity →</Link></p></aside></div></article>}
